/**
 * 处理请求 `body`，即将`data`转换为JSON字符串
 * @param data
 */
export function transformRequest(data: any): any {
  // TODO

  return data
}

/**
 * 处理响应 `data`
 * @param data
 */
export function transfromResponse(data: any): any {
  // TODO

  return data
}
